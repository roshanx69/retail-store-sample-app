apiVersion: apps/v1
kind: Deployment
metadata:
  name: dynamodb-local
  namespace: store-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dynamodb-local
  template:
    metadata:
      labels:
        app: dynamodb-local
    spec:
      containers:
      - name: dynamodb-local
        image: amazon/dynamodb-local
        ports:
        - containerPort: 8000
        command: ["-jar", "DynamoDBLocal.jar", "-inMemory", "-sharedDb"]
---
apiVersion: v1
kind: Service
metadata:
  name: dynamodb-local
  namespace: store-app
spec:
  selector:
    app: dynamodb-local
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
